// npm

/*
 [npm(node package manager)]
 Javascript 패키지 저장소

 [package.json]
 package name, pacakge version 정보 기록


 npm -v
 npm update -g npm : 최신 버전으로 업데이트
 npm init : package.json 생성
 npm install : package.json 파일 이동 후 명령어 입력시  그 안에 들어있는 모든 패키지 설치
 npm install [package] : 패키지 설치
 npm install [package] --save : 패키지 설치 후 package.json 파일에 저장, (루트 디렉토리로 이동 후 실행)
 const [variable] = require(package); : 변수명으로 해당 패키지 사용
 */

